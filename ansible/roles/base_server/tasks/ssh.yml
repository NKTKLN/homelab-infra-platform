---
# Harden SSH daemon configuration using a managed template.

- name: Harden SSH configuration
  template:
    src: sshd_config.j2
    dest: /etc/ssh/sshd_config
    mode: 0600
  notify: restart ssh
  when: common_harden_ssh | bool
