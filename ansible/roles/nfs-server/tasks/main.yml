---
# Main task file for the nfs-server role.
# Includes logically separated task files grouped by feature.

- include_tasks: setup.yml
  tags: [setup]

- include_tasks: configure.yml
  tags: [configure]
